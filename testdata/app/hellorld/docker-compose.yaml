version: '2.3'
services:
  hellorld:
    image: "busybox:stable"
    command:
    - /bin/sh
    - "-c"
    - "mkdir /www && echo Hellorld!>/www/index.html && httpd -f -p 5001 -h /www"
    ports:
    #  - "5001:5001"
